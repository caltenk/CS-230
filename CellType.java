/**
 * An enumeration of all cell types.
 *
 * @author George Manning.
 */
public enum CellType {
    WALL, GROUND, GOAL, WATER, FIRE, RED_DOOR, GREEN_DOOR, BLUE_DOOR, TOKEN,
    FLIPPERS, FIREBOOTS, RED_KEY, GREEN_KEY, BLUE_KEY, TOKEN_DOOR, TELEPORTER;
}
